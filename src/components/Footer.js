import React from 'react';
import { Typography } from '@material-ui/core';

const Footer = () => {
    return(
        <footer className="footer-text">
            <Typography variant="body2">Built in React.js by Fabian Fabro.</Typography>
        </footer>
    )
}

export default Footer;